global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'sglang'
    static_configs:
      - targets: ['sglang-server:30000']
  
  - job_name: 'pushgateway'
    honor_labels: true
    static_configs:
      - targets: ['pushgateway:9091']
