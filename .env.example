# ==============================================================================
# .env — mlsys-marconi Docker configuration
#
# Copy to .env and edit:  cp .env.example .env
# ==============================================================================

# --- Model --------------------------------------------------------------------
# HuggingFace model ID. NVIDIA Nemotron-H (hybrid Mamba–Transformer):
#   nvidia/Nemotron-H-8B-Base-8K
#   nvidia/Nemotron-H-47B-Base-8K
#   nvidia/Nemotron-H-56B-Base-8K
MODEL_NAME=nvidia/Nemotron-H-8B-Base-8K

# Tensor parallel size (number of GPUs)
TENSOR_PARALLEL=1

# HuggingFace token (for gated models if required)
HF_TOKEN=

# Extra arguments for sglang.launch_server
# e.g. --disable-radix-cache  or  --marconi-evict-policy 2 --marconi-eff-weight 0.5
SGLANG_EXTRA_ARGS=

# --- Networking ---------------------------------------------------------------
SGLANG_PORT=30000
